import {NextResponse} from "next/server";import {currentUser} from "@/lib/auth";import {db} from "@/lib/db";
export async function POST(req:Request){const u=await currentUser();if(!u||u.role!=="RECRUITER"||!u.recruiter)return NextResponse.json({error:"Recruiters only"},{status:403});const b=await req.json();const job=await db.job.create({data:{recruiterId:u.recruiter.id,title:b.title,description:b.description,type:b.type,location:b.location,deadline:new Date(b.deadline),qualifications:b.qualifications,requiredSkills:b.requiredSkills,preferredSkills:b.preferredSkills||"",vacancies:Number(b.vacancies)||1}});return NextResponse.json(job)}
